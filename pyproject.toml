[tool.poetry]
name = "csmp-thesis-project"
version = "0.1.0"
description = ""
authors = ["IVproger <i.golov@innopolis.university>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
